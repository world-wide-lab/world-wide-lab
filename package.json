{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "jest",
    "build": "npm run build --prefix packages/wwl/"
  },
  "devDependencies": {
    "jest": "^29.5.0"
  },
  "jest": {
    "projects": [
      "<rootDir>/packages/wwl"
    ]
  }
}
