{
  "name": "@world-wide-lab/electron-app",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "electron .",
    "build": "npm run build:wwl && npm run package",
    "build:dev": "npm run build:wwl && npm run package:dev",
    "build:wwl": "npm run build --prefix ../server/",
    "package": "electron-builder",
    "package:dev": "electron-builder --dir",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "NO_LICENSE_YET",
  "dependencies": {
    "@world-wide-lab/server": "file:../server"
  },
  "devDependencies": {
    "electron": "^24.6.1",
    "electron-builder": "^24.4.0"
  }
}
